# Backend API base URL (includes /api/v1)
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Optional: public uploads base (defaults to API host + /uploads)
# NEXT_PUBLIC_UPLOADS_URL=http://localhost:5000/uploads
